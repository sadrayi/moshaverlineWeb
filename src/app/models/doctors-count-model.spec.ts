import { DoctorsCountModel } from './doctors-count-model';

describe('DoctorsCountModel', () => {
  it('should create an instance', () => {
    expect(new DoctorsCountModel()).toBeTruthy();
  });
});
